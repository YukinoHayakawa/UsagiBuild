<Project>
  <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)..\'))" />
  <PropertyGroup>
    <Dir_IncludePath>$(SolutionDir)Library;$(Dir_IncludePath)</Dir_IncludePath>
  </PropertyGroup>
</Project>